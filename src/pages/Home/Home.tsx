import { MessageBubble } from "../../components";

const Home = () => {
  return (
    <div className="px-10 py-4 h-full flex flex-col items-center">
      <MessageBubble />
    </div>
  );
};

export default Home;
